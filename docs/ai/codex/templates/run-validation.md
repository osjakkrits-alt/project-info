# Run Validation

## Scope

Validate `<branch, diff, feature, or paths>` against `<acceptance criteria or quality requirements>`.

## Instructions

Inspect repository documentation and configuration to discover available checks. Run applicable formatting, linting, tests, builds, type checks, documentation checks, and security checks without altering behavior. Do not suppress failures or modify files unless explicitly requested.

## Expected checks

- `<command or check to discover>`
- `<command or check to discover>`

## Output

Report each check, command, and result. Include concise failure details, distinguish implementation failures from environment limitations, and list checks that were unavailable, skipped, or blocked with reasons. Do not commit or push.
