# Tests

Place automated tests and test support files here, or document the ecosystem-specific test locations if tests live beside source files.

Record how to run unit, integration, end-to-end, and other checks once a toolchain is selected. Tests should be deterministic and must not require committed secrets.
