# Copy this file to the environment file expected by the project.
# Use placeholders only; never commit secrets.

APP_ENV=<environment-name>
APP_PORT=<port-number>
SERVICE_BASE_URL=<service-base-url>
SERVICE_API_KEY=<local-secret-value>
